server {
    listen 80;
    server_name _;

    root __PROJECT_DIR__/frontend/dist;
    index index.html;

    # Serve the Vue SPA — all routes fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
